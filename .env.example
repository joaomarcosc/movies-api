# 🛢️ Database Configuration
DB_HOST=               # Database host (e.g., localhost, db.production)
DB_PORT=               # Database port (e.g., 5432 for PostgreSQL)
DB_USERNAME=           # Database username
DB_PASSWORD=           # Database password
DB_NAME=               # Database name

# 🚀 Application Configuration
PORT=                  # Application port (e.g., 3000)

# 🔐 JWT Authentication
JWT_SECRET=            # Secret key used to sign and verify JWT tokens
JWT_EXPIRATION=        # JWT token expiration time (e.g., 1h, 7d)

# ☁️ AWS S3 Configuration
AWS_ACCESS_KEY_ID=     # AWS access key ID
AWS_SECRET_ACCESS_KEY= # AWS secret access key
AWS_REGION=            # AWS region (e.g., sa-east-1 for South America)
AWS_S3_BUCKET_NAME=    # AWS S3 bucket name for file storage

# 📧 Email Configuration (Resend)
RESEND_API_KEY=        # Resend API key for sending emails
EMAIL_FROM=            # Default sender email address (e.g., onboarding@resend.dev)
